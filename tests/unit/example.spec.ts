import { shallowMount } from '@vue/test-utils'

describe('example', () => {
  // let wrapper = shallowMount(HelloWorld); // setting the type

  it('demo example', () => {
    
    expect(1 + 1).toBe(2)
  })
})
